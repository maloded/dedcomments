# Backend GraphQL endpoint. Public (NEXT_PUBLIC_*) because Apollo Client runs in the
# browser — this is the same URL a curl/Postman request would use, nothing secret.
NEXT_PUBLIC_GRAPHQL_URL=http://localhost:4000/graphql
