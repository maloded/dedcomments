/** 1-based index of the first page. */
export const DEFAULT_PAGE = 1;

/**
 * Brief §3: the home-page table of top-level comments is paginated at
 * 25 messages per page (pagination applies only to root comments).
 */
export const ROOT_COMMENTS_PER_PAGE = 25;
